# lucent
A lightweight local image upscaler built for personal use. It uses pretrained super-resolution models to increase image resolution while preserving and reconstructing fine details. Designed to run entirely on your machine with GPU acceleration, keeping images private and avoiding the overhead of cloud-based processing.
